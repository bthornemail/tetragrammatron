# Bridge

Adapter modules that connect mechanical layers without redefining semantics.

`unicode-control-bridge.mjs` bridges:

- `src/control/surface.mjs`
- `src/unicode-geometry/`

It does not invoke EABI and does not interpret ABI structures.
